/** Anzeigedauer pro Kommentar (ms), bevor der Text ausgeblendet wird. */
export const COMMENTATOR_DISPLAY_MS = 8000;

/** Kurze Pause beim Verlassen, damit der GAME_ENDED-Kommentar sichtbar bleibt. */
export const COMMENTATOR_GAME_END_LEAVE_MS = 2500;
